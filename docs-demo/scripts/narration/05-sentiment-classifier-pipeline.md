# Sentiment Classifier Pipeline Narration Skeleton

Status: every pipeline step is to-verify until evidence is mapped and recorded.

Opening:
- This is the official `sentiment-classifier` sample from `backend/data/demo_samples/sentiment-classifier`.
- The manifest says the task profile is `classification`.
- The source CSV uses `text` and `label`.
- The label set is positive, neutral, negative.

To verify in UI:
1. Demo tile selection.
2. Raw reviews and labels.
3. Gold rows.
4. Dataset prep and labels in manifest.
5. Tokenization.
6. Training run.
7. Evaluation.
8. Export, especially any mobile CPU or ONNX-INT8 path.

Narration caveat:
- The manifest describes the target/export story, but the actual export path must be proven before claiming it.

