# Support FAQ Pipeline Narration Skeleton

Status: every pipeline step is to-verify until evidence is mapped and recorded.

Opening:
- This is the official `support-faq` sample from `backend/data/demo_samples/support-faq`.
- The manifest says the task profile is `instruction_sft`.
- The source CSV uses `question` and `answer`.

To verify in UI:
1. Demo tile selection.
2. Raw Q&A rows.
3. Gold rows from `gold.jsonl`.
4. Prepared train/val/test split.
5. Training configuration.
6. Evaluation.
7. Export or final usage.

Narration rule:
- If a stage is seeded, say it is seeded.
- If a stage is manually run, say it is run.
- If a stage is only conceptual, say it is conceptual.

