# SLM 101 — Narration

Status: **synced** with the actual narrated take produced by
`tts/generate_v01_narration.py` (Orpheus voice "leo") on 2026-05-20.

The **Python script** at
[tts/generate_v01_narration.py](../../../tts/generate_v01_narration.py)
is the **authoritative source** of the spoken text. Slides at
[docs-demo/slides/v01-slm-101.html](../../slides/v01-slm-101.html)
are paired one-to-one with the audio sections.

Total runtime: **1:55** (matches
`docs-demo/recordings/raw/01-slm-101-narrated.mp4`).

First non-product video. Conceptual intro to small language models.
Content grounded in
[docs-demo/learning-path/01-slm-101.md](../../learning-path/01-slm-101.md).

---

## Slide 1 — Title (0:00–0:12)

> "Welcome to SLM one-oh-one. A short intro before we touch the
> product. Five minutes. By the end you'll know what a small
> language model is, why anyone bothers, and what shape the
> platform demo will take."

## Slide 2 — Small is relative (0:12–0:31)

> "What's a small language model? It's a language model designed
> to be smaller, cheaper, faster, or easier to deploy than a big
> general-purpose one. Small is relative. It can mean fewer
> parameters, lower memory use, shorter context, a narrower task,
> or a smaller deployment target. There isn't a hard cutoff between
> LLM and SLM — there's a spectrum."

## Slide 3 — Why smaller models matter (0:31–0:53)

> "Why smaller models matter. Five reasons. Lower serving cost —
> you don't need a fleet of H-one-hundreds to run inference. Lower
> latency — small models respond in milliseconds. Easier on-device
> or private deployment — the model fits on a laptop or a phone.
> Better control for narrow tasks — a model tuned for one job
> often beats a general-purpose one. And a smaller blast radius
> when something goes wrong."

## Slide 4 — The lifecycle (0:53–1:17)

> "The lifecycle. Define the task. Collect or import data. Clean
> it. Build a gold set — examples you trust to judge quality
> with. Generate synthetic examples if the source is small.
> Prepare train, validation, and test splits. Pick a base model.
> Configure and run training. Evaluate against the gold set.
> Compress and export. Then test the result in a real usage path.
> That's the loop the rest of these videos walk."

## Slide 5 — Where BrewSLM fits (1:17–1:38)

> "Where BrewSLM fits in this picture. It's a workspace for
> moving through that lifecycle on local hardware. Data,
> cleaning, gold sets, synthetic generation, dataset preparation,
> tokenization, training, evaluation, compression, export,
> registry, usage. Every stage has a tab. Every action leaves an
> artifact on disk. Nothing needs the cloud."

## Slide 6 — Up next (1:38–1:55)

> "That's the intro. Next video opens the platform, seeds a demo
> project, and lands on the data tab in under five minutes. After
> that we walk three sample pipelines — support FAQ, PII
> detection, and sentiment classification — and then run a real
> training-to-serving loop on local hardware."

---

## Why a slide deck (not the product) for this video

Video 01 is conceptual. Nothing concrete to demo yet — no project
to open, no row to expand. Forcing the product UI on it would
either repeat the V02 quickstart (boring) or invent content that
isn't real ("here's a 'SLM definition' tab"). A clean six-slide
deck respects the viewer's time and lines up with the
[learning-path/01-slm-101.md](../../learning-path/01-slm-101.md)
beginner doc the platform already ships.

The recording pipeline is identical to the product videos: same
Orpheus voice, same Playwright spec structure, same ffmpeg mux.
The only difference is the spec loads `file://` instead of
`localhost:5173` and calls `window.showSlide(n)` instead of
scrolling content into view.
