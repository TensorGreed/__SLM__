# Pipeline Step Evidence

Discovery date: 2026-05-19.

This table is intentionally conservative. A step is not treated as supported just
because it is common in ML pipelines. Verified and partial rows list exact repo
evidence and recording implications.

Status legend: verified, partial, simulated, estimated, conceptual, unsupported,
unknown.

| Pipeline step | Classification | Exact evidence files | Route/page/component if UI exists | Backend API/service if backend exists | Sample-specific behavior | What can be recorded in browser | What requires API/CLI | Heavy runtime or external dependency | What viewer should see | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| project setup | verified | `README.md`, `backend/app/config.py`, `backend/.env.example`, `frontend/src/App.tsx`, `backend/app/main.py` | `/login`, `/`, `/project/:id` | API mounted under `/api`; auth handled by `authorize_request` | None sample-specific | Login, project list, workspace shell | Backend/frontend/worker startup commands | Redis/Celery for queued jobs; optional GPU/model deps later | Running local app and authenticated project workspace | Auth enabled by default; default local password is config/API key dependent |
| sample selection/import | verified | `backend/data/demo_samples/*/manifest.json`, `backend/app/services/demo_project_service.py`, `backend/app/api/demo_projects.py`, `frontend/src/components/dashboard/DemoProjectTiles.tsx` | Project list demo tiles | `GET /api/demo-projects`, `POST /api/demo-projects/{slug}` | Only `support-faq`, `pii-detector`, `sentiment-classifier` are official | Three demo tiles, click to create/open project | Direct POST seed API if UI is unavailable | Running backend/frontend/auth | Sample appears in dashboard and opens as project | Do not invent templates |
| dataset ingestion | verified | `frontend/src/components/data/IngestionPanel.tsx`, `backend/app/api/ingestion.py`, `backend/app/services/demo_project_service.py` | `/project/{id}/pipeline/data` | `/api/projects/{id}/ingestion/upload`, upload-batch, remote import, documents, EDA | Seeder creates one raw document per sample CSV row | Data tab upload/remote/document list/sample surfaces | Remote import queue/status/cancel APIs; seed API | Remote HF/Kaggle may need deps/credentials; queued import needs Redis/Celery | Raw rows/documents exist for seeded sample | Dataset import wizard is separate and writes to synthetic output |
| raw data inspection | verified | `frontend/src/components/data/IngestionPanel.tsx`, `frontend/src/components/data/DocumentSampleAccordion.tsx`, `backend/app/api/ingestion.py` | Data tab document list/sample accordion | `GET /api/projects/{id}/ingestion/documents`, `GET /api/projects/{id}/ingestion/documents/{document_id}/sample` | Support rows show Q&A, PII rows show `text/entities_json`, sentiment rows show `text/label` if exposed | Expand document/sample preview | Direct document/sample API if UI does not show row detail | None for seeded rows | Raw sample rows before transformation | Durable selectors still need UI pass |
| data cleaning | verified | `frontend/src/components/data/CleaningPanel.tsx`, `backend/app/api/cleaning.py`, `backend/app/services/cleaning_service.py` | `/project/{id}/pipeline/cleaning` | `/clean`, `/clean-batch`, `/clean-batch-async`, `/tasks/{task_id}`, `/chunks` | Applies to raw documents from all samples; seeded prepared splits already exist separately | Configure chunk size/redaction/toxicity, start cleaning, see quality/chunks/PII/toxicity | Direct clean APIs; inspect generated cleaned files | None for regex/local cleanup; batch async is in-process | Cleaning results and metadata | Route rendering verified; actual cleaning action still not run |
| normalization | partial | `backend/app/services/demo_project_service.py`, `backend/app/services/dataset_service.py`, `backend/app/api/dataset.py`, `frontend/src/components/data/DatasetPrepPanel.tsx`, `backend/app/services/dataset_import/service.py` | Dataset prep adapter preview/profile/split; dataset import wizard | `/dataset/adapters/preview`, `/dataset/profile`, `/dataset/split`, dataset-import preview/run | Seeder canonicalizes all rows; sentiment keeps label; PII keeps schema; support maps to Q&A | Adapter preview, prepared preview, split config | Inspect prepared manifest/files if UI omits detail | Domain normalizer hooks depend on selected profile; external imports may need deps | Raw rows become task-shaped training rows | There is no single verified "normalize" button; it is embedded in import/prep |
| deduplication | partial | `backend/app/services/cleaning_service.py`, `backend/app/services/gold_workbench_service.py`, `backend/app/services/dataset_service.py`, `slm-docs/docs/workflows/data-ingestion.md` | Gold workbench; dataset prep semantic analysis | Gold workbench sampling; semantic analysis endpoints | No sample-specific duplicate-removal behavior verified | Possibly show gold workbench skipped duplicates or semantic redundancy if present | API/file inspection for `text_hash` and workbench sample results | Semantic analysis may need embedding/vector support | Viewer can see duplicate signals, not proven automatic row removal | Cleaning computes `text_hash`; automatic cleaning-stage dedup removal is unsupported/unknown |
| PII detection/removal/masking | partial | `backend/data/demo_samples/pii-detector/*`, `backend/app/services/cleaning_service.py`, `backend/app/services/synthetic_service.py`, `backend/app/services/eval_task_handler_service.py`, `frontend/src/components/data/CleaningPanel.tsx`, `frontend/src/components/data/SyntheticPanel.tsx` | Cleaning tab; PII sample pipeline; synthetic span mode | Cleaning APIs; synthetic span APIs; structured extraction eval handler | `pii-detector` is span extraction; cleaning redacts regex PII as `[REDACTED_TYPE]` | Show cleaning redaction settings; show PII sample entity schema/spans | Real span generation/eval/training via APIs after runtime config | Teacher model for real synthetic spans; model runtime for detector; no heavy deps for regex redaction | Difference between redacting PII in cleanup and training/evaluating a detector | Do not claim model-based masking/removal unless separately proven |
| gold set creation/import | verified | `frontend/src/components/data/GoldSetPanel.tsx`, `frontend/src/components/evaluation/GoldSetWorkbenchPanel.tsx`, `backend/app/api/gold.py`, `backend/app/services/gold_service.py`, `backend/app/services/gold_workbench_service.py`, `backend/app/services/demo_project_service.py` | `/project/{id}/pipeline/goldset`; eval workbench component | Legacy `/gold/add`, `/gold/import`, `/gold/lock`; workbench service APIs | Seeder imports 200-row gold JSONL into locked approved gold rows for all samples | Gold rows, lock state, workbench sampling/review if visible | Direct gold APIs if UI omits details | None for local gold rows | Trusted evaluation examples separate from raw training rows | Legacy panel is Q&A-shaped; workbench is more general |
| synthetic data generation | partial | `frontend/src/components/data/SyntheticPanel.tsx`, `backend/app/api/synthetic.py`, `backend/app/services/synthetic_service.py` | `/project/{id}/pipeline/synthetic` | `/generate`, `/save`, `/generate-conversations`, `/save-conversations`, `/generate-spans`, `/generate-spans-async`, `/save-spans` | PII can auto-switch to span mode from prepared manifest; support can use Q&A/conversation; sentiment-specific classification synthetic path not verified | Synthetic UI modes, provider config, async span progress, save generated rows | Direct synthetic APIs; env toggles | Teacher model URL/API key or explicit demo fallback; async span path may sample cleaned chunks | Generated examples if real/fallback generator configured | Fallback must be labeled simulated/demo |
| dataset preparation | verified | `frontend/src/components/data/DatasetPrepPanel.tsx`, `backend/app/api/dataset.py`, `backend/app/services/dataset_service.py`, `backend/app/services/demo_project_service.py` | `/project/{id}/pipeline/dataprep` | `/dataset/preview`, `/dataset/adapters/catalog`, `/dataset/adapters/preview`, `/dataset/profile`, `/dataset/split` | Seeder already creates prepared files; manual prep can rebuild from selected dataset types | Adapter catalog/preview, profile, semantic analysis, split controls, prepared preview | Direct dataset APIs; inspect prepared files | Semantic analysis may have embedding/dependency constraints | Chosen data becomes task-ready train/val/test files | Must explain seeded vs manually prepared data |
| train/validation/test split | verified | `backend/app/services/demo_project_service.py`, `backend/app/services/dataset_service.py`, `frontend/src/components/data/DatasetPrepPanel.tsx` | Dataset prep split tab | `/dataset/split`, `/dataset/split/effective-config` | Support 16/2/2; PII 45/8/8; sentiment 22/4/4 from seeder logic | Split config and resulting prepared datasets if UI shows counts | Direct split API; file inspection | None | Train, validation, and test partitions | Seeder split is deterministic and not shuffled; manual split shuffles with seed |
| tokenization | verified | `frontend/src/components/training/TokenizationPanel.tsx`, `backend/app/api/tokenization.py`, `backend/app/services/tokenization_service.py` | `/project/{id}/pipeline/tokenization` | `/tokenization/analyze`, `/tokenization/vocab-sample` | Uses prepared split files; sample-specific text length distributions differ | Tokenizer preset, max sequence length, histogram, stats, vocab sample | Direct tokenization APIs | `transformers`; tokenizer download/cache; gated HF models may require auth | Token lengths and recommended sequence length | UI feature is verified; successful analysis depends on tokenizer availability |
| training configuration | verified | `frontend/src/pages/ProjectTrainingConfigPage.tsx`, `frontend/src/components/training/TrainingPanel.tsx`, `backend/app/api/training.py`, `backend/app/services/training_service.py` | `/project/{id}/training-config`; training tab | runtime list, recipes, effective config, preflight, experiment APIs | Manifest preferred plan profile influences seeded project settings | Config form, runtime choice, preflight output | Direct training config/preflight APIs | Depends on runtime and data artifacts | Training plan before execution | Route rendering verified; full form selectors still need hardening |
| training run | partial | `backend/app/services/training_runtime_service.py`, `backend/app/services/training_service.py`, `backend/app/api/training.py`, `backend/scripts/train.py`, `frontend/src/components/training/TrainingPanel.tsx` | Training tab | `/training/experiments/{id}/start`, status/tasks/run manifest endpoints | Official samples have prepared train/val files; task adapter drives train script contract | Start button and run progress only after runtime setup | Worker command, env config, direct start/status APIs | Default external runtime likely needs Redis/Celery, torch/transformers/datasets/accelerate, model access; simulation requires explicit env | Real or explicitly simulated training progress | Simulated training is off by default |
| training monitor/logs | partial | `frontend/src/components/training/TrainingPanel.tsx`, `backend/app/api/training.py`, run event services | Training tab, observability surfaces | status/tasks/run manifest/checkpoints/run-events endpoints | No sample-specific monitor behavior found | Metrics/log timeline after a run starts | Direct status/task/log APIs | Same as training run | Loss/metrics/checkpoints/logs if produced | Requires a configured run |
| evaluation | partial | `frontend/src/components/evaluation/EvalPanel.tsx`, `backend/app/api/evaluation.py`, `backend/app/services/evaluation_service.py`, `backend/app/services/eval_task_handler_service.py` | Eval tab | `/evaluation/run`, `/evaluation/run-heldout`, `/evaluation/results/{experiment_id}`, `/evaluation/llm-judge` | Sentiment classification handler; PII structured extraction span handler; support handler mapping still open | Workbench/eval controls/results after predictions/model exist | Direct eval APIs; supplied predictions possible | Held-out model eval needs local/remote inference; LLM judge needs judge endpoint or fallback path | Metrics for task-specific predictions | Do not claim measured results until run |
| scorecard/report | partial | `frontend/src/components/evaluation/EvalPanel.tsx`, `backend/app/api/evaluation.py`, `backend/app/services/evaluation_pack_service.py` | Eval tab | `/evaluation/scorecard/{experiment_id}`, `/evaluation/safety-scorecard/{experiment_id}` | Classification pack supports accuracy/macro-F1; structured extraction supports span metrics; support pack uncertain | Scorecard/report panel after eval | Direct scorecard APIs | Requires evaluation records | Summary metrics, pass/fail details | Available surface, not measured in this pass |
| quality gates | partial | `backend/app/api/evaluation.py`, `backend/app/services/evaluation_pack_service.py`, `backend/app/services/export_service.py`, `backend/app/services/registry_service.py`, `frontend/src/pages/ProjectPipelinePage.tsx` | Eval/export/registry related UI | `/evaluation/gates/{experiment_id}`, project gate checks, export gate check, registry promotion gates | Manifest eval pack preference differs by sample | Gate result after eval/export/registry action | Direct gate APIs | Requires metrics/eval data; production promotion may require safety/regression evidence | Pass/fail gates with actionable reasons | Export can block on gate policy if configured |
| compression | partial | `frontend/src/components/compression/CompressionPanel.tsx`, `backend/app/api/compression.py`, `backend/app/services/compression_service.py`, `backend/scripts/quantize.py`, `backend/scripts/benchmark.py`, `backend/scripts/model_merge.py` | Compression tab | `/compression/quantize`, `/merge-lora`, `/benchmark`, `/merge-models`, job status/task APIs | Sentiment/mobile story likely points to ONNX/INT8, but not run | Quantize/benchmark/merge controls and job status | Worker/env setup; direct compression APIs | Redis/Celery; llama.cpp, optimum/onnxruntime, transformers/torch; stub disabled unless env allows | Compressed artifact and benchmark only after real run | Model merge script has fallback simulation behavior; label if used |
| export | partial | `frontend/src/components/export/ExportPanel.tsx`, `backend/app/api/export.py`, `backend/app/services/export_service.py`, `backend/app/models/export.py` | Export tab | `/export/create`, `/export/{id}/run`, deployment validation, serve plan APIs | Sentiment manifest intent mentions ONNX-INT8; PII/support likely HF/GGUF/vLLM depending target | Export format selection, run status, manifest/artifacts, serve plan | Direct export APIs; inspect package dir | Requires trained/compressed artifacts; target validation may require local tools | Export package with manifest, Dockerfile, `serve.py`, release notes | Formats verified: `gguf`, `onnx`, `tensorrt`, `huggingface`, `docker` |
| model registry | partial | `frontend/src/components/export/ExportPanel.tsx`, `backend/app/api/registry.py`, `backend/app/services/registry_service.py` | Export panel registry area | `/registry/models/register`, `/registry/models`, `/readiness/{experiment_id}`, `/promote`, `/deploy`, serve-plan/run APIs | No sample-specific registry behavior found | Register/promote/deploy controls after experiment/export exists | Direct registry APIs | Requires experiment/artifact; promotion gates may require metrics/safety | Candidate/staging/production model record and readiness | Production deploy can be blocked by gates |
| final model testing | partial | `frontend/src/pages/ProjectPlaygroundPage.tsx`, `frontend/src/components/training/ChatPlaygroundPanel.tsx`, `backend/app/services/playground_service.py`, `backend/app/services/playground_session_service.py` | `/project/{id}/playground` | Playground model/providers/sessions/chat/log/feedback endpoints | No sample-specific prompts verified; model options can include registry/experiment/artifact items | Prompt playground with model/provider selection and response | Direct playground chat API; start local endpoint if needed | Mock is simulated; real endpoint needs Ollama/vLLM/TGI/llama.cpp/OpenAI-compatible service | A prompt and model response with feedback logging | A mock response is not proof of trained model quality |
| final model API usage | partial | `backend/app/services/export_service.py`, `backend/app/services/serve_service.py`, `backend/app/services/serve_runtime_service.py`, `backend/app/api/export.py`, `backend/app/api/registry.py` | Export panel serve plan/run area | export/registry serve-plan and serve-runs start/status/stop | No sample-specific API route found beyond generated serve plan | Serve plan command/curl and local serve status after export | Start subprocess or run generated curl/smoke request | Exported artifact and runtime-specific tool/server | Concrete API request to served model | API usage remains unproven until export/serve succeeds |
| final model UI usage | partial | `frontend/src/pages/ProjectPlaygroundPage.tsx`, `frontend/src/components/training/ChatPlaygroundPanel.tsx`, `frontend/src/components/export/ExportPanel.tsx`, `frontend/src/pages/ProjectDeploymentsPage.tsx` | Playground, export, deployments | Playground/export/deployment APIs | Unknown whether newly trained model appears automatically without export/registry linkage | Select model/runtime and test in playground if model option exists | Direct model option/serve APIs if UI path is unclear | Real runtime endpoint or artifact | UI call against trained/exported model | Automatic linkage is unknown |
| deployment/runtime usage | partial | `frontend/src/pages/ProjectDeploymentsPage.tsx`, `frontend/src/components/export/ExportPanel.tsx`, `backend/app/api/deployments.py`, `backend/app/services/deployment_target_service.py`, `backend/app/services/serve_runtime_service.py` | `/project/{id}/deployments`; export deploy/serve areas | deployment versions, telemetry, drift, score APIs; deploy-as-api; serve-run APIs | No sample-specific deployment path found | Deployment dashboard, serve status, telemetry/drift if data exists | Direct deploy/telemetry APIs; external deploy commands | Docker/vLLM/TGI/Ollama/llama.cpp/ONNX/TensorRT/HF endpoint depending target | Runtime endpoint, health, smoke test, telemetry | Telemetry is data-dependent; remote deployment may need credentials |

## Selector/Route Evidence Addendum

Status: verified for browser route rendering and seed/import surfaces; partial
for runtime-heavy execution.

Disposable UI passes on 2026-05-19 confirmed the following without running
training, evaluation, compression, export, or deployment:

| Sample | Project id | Routes verified | Raw docs | Gold rows | Prepared split | Adapter/task evidence | Schema/label evidence | Screenshots |
|---|---:|---|---:|---:|---|---|---|---|
| `support-faq` | 1 | data, cleaning, goldset, synthetic, dataprep, tokenization, training, eval, compression, export, training-config | 20 | 200 | 16/2/2 | `qa-pair`, `instruction_sft` from prepared manifest | field mapping `question` to `answer` | `selector-pass-03-support-faq-data-tab.png` through `selector-pass-14-training-config.png` |
| `pii-detector` | 1 in `/tmp/slm-selector-pass-remaining.8c6MCJ` | data, cleaning, goldset, synthetic, dataprep, tokenization, training, eval, compression, export, training-config | 61 | 200 | 45/8/8 | `structured-extraction`, `structured_extraction` from prepared manifest | `span_set` output schema and entity types from prepared manifest | `selector-pass-pii-02-data-tab.png` through `selector-pass-pii-13-training-config.png` |
| `sentiment-classifier` | 2 in `/tmp/slm-selector-pass-remaining.8c6MCJ` | data, cleaning, goldset, synthetic, dataprep, tokenization, training, eval, compression, export, training-config | 30 | 200 | 22/4/4 | `classification-label`, `classification` from prepared manifest | labels `positive`, `neutral`, `negative` from prepared manifest | `selector-pass-sentiment-02-data-tab.png` through `selector-pass-sentiment-13-training-config.png` |

Selector evidence now verified:

- Login can use placeholders `Enter your username` and `API Key or Password`,
  plus the `Sign in` button role.
- Demo catalog can use `.demo-project-tiles` and `.demo-project-tile`.
- Tile selection should filter by visible text such as `Support FAQ`,
  `PII / PCI Detector`, or `Sentiment classifier`; exact accessible names use
  `Demo · ...` punctuation from manifests.
- Raw row expansion can use `[data-testid^="expand-doc-"]`; observed concrete
  ids were `expand-doc-20`, `expand-doc-61`, and `expand-doc-91`.
- Pipeline tab buttons rendered with title values `Data`, `Cleaning`,
  `Gold Set`, `Synthetic`, `Dataset Prep`, `Tokenization`, `Training`,
  `Evaluation`, `Compression`, and `Export`.

Runtime warnings seen in browser:

- Data tab system readiness showed `WARN` with missing
  `TEACHER_MODEL_API_KEY`.
- Synthetic tab showed no text loaded yet until cleaned chunks are loaded.
- Training tab showed no experiments yet.
- Evaluation tab showed no experiments to evaluate.
- Export tab rendered target/deployment copy, but no export artifact existed.

Recording consequence:

- Sample selection, raw inspection, gold set inspection, prepared-manifest
  evidence, and route navigation are verified for all official samples.
- Training run, evaluation metrics, scorecards, gates, compression artifacts,
  export packages, registry promotion, and final model use remain partial until
  a controlled runtime pass creates real or explicitly simulated artifacts.
