import { test } from '@playwright/test';

test.skip('support FAQ recording placeholder', async () => {
  // This recording must not be implemented until the sample files, UI routes,
  // APIs, and real pipeline steps are mapped in docs-demo/evidence.
});

