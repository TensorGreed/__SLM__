---
sidebar_position: 7
title: Autopilot
---

<!-- Auto-generated by slm-docs/scripts/generate_api_ref.py. Do not edit by hand — re-run the generator instead. -->

# Autopilot

Auto-generated reference for the **Autopilot** API. 10 endpoint(s).

For curated narrative + UI / CLI walkthroughs see the corresponding section under
[Pipeline workflows](../../workflows/pipeline-overview.md), [Deployment](../../deployment/plan.md),
[Observability](../../observability/run-events.md), or [Extensions](../../extensions/contracts.md).

### `GET /api/autopilot/decisions`

**List Autopilot Decisions**

List autopilot decision-log entries with optional filters.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `project_id` | `query` | `integer \\| null` | no | Filter by project. |
| `run_id` | `query` | `string \\| null` | no | Filter by autopilot run id. |
| `stage` | `query` | `string \\| null` | no | Filter by pipeline stage / step. |
| `status` | `query` | `string \\| null` | no | Filter by decision status. |
| `action` | `query` | `string \\| null` | no | Filter by derived action. |
| `reason_code` | `query` | `string \\| null` | no | Filter by reason code. |
| `since` | `query` | `string(date-time) \\| null` | no | Created at &gt;= (ISO-8601). |
| `until` | `query` | `string(date-time) \\| null` | no | Created at &lt;= (ISO-8601). |
| `limit` | `query` | `integer` | no |  |
| `offset` | `query` | `integer` | no |  |

**Responses**

| Status | Schema | Description |
|---|---|---|
| `200` | `any` | Successful Response |
| `422` | `HTTPValidationError` | Validation Error |


### `POST /api/autopilot/repair-apply`

**Autopilot Repair Apply**

Execute a previously previewed plan, after state-drift re-validation.

Refuses with 404/409/410 when the plan is unknown, already applied,
expired, or the project state has changed since the preview was captured
(see `_APPLY_FAILURE_STATUS` for the mapping). Pass `force=true` to skip
state-drift detection.

**Request body** (required)

Content type: `application/json` — `AutopilotRepairApplyRequest`

| Field | Type | Required | Description |
|---|---|---|---|
| `plan_token` | `string` | yes |  |
| `actor` | `string \\| null` | no |  |
| `reason` | `string \\| null` | no |  |
| `expected_state_hash` | `string \\| null` | no |  |
| `force` | `boolean` | no |  |

**Responses**

| Status | Schema | Description |
|---|---|---|
| `200` | `any` | Successful Response |
| `422` | `HTTPValidationError` | Validation Error |


### `POST /api/autopilot/repair-preview`

**Autopilot Repair Preview**

Dry-run the autopilot plan, persist the result, and return a plan token.

The body accepts every field of `AutopilotV2OrchestrationRequest` plus a
top-level `project_id`. Any `dry_run` value in the body is ignored — the
preview is always dry. Use the returned `plan_token` with
`POST /autopilot/repair-apply` to actually execute the plan.

**Request body** (required)

Content type: `application/json` — `object`

**Responses**

| Status | Schema | Description |
|---|---|---|
| `200` | `any` | Successful Response |
| `422` | `HTTPValidationError` | Validation Error |


### `GET /api/autopilot/snapshots`

**List Autopilot Snapshots**

List captured autopilot snapshots (rollback candidates).

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `project_id` | `query` | `integer \\| null` | no | Filter by project. |
| `run_id` | `query` | `string \\| null` | no | Filter by autopilot run id. |
| `include_restored` | `query` | `boolean` | no | Include already-rolled-back snapshots. |
| `include_expired` | `query` | `boolean` | no | Include expired snapshots. |
| `limit` | `query` | `integer` | no |  |
| `offset` | `query` | `integer` | no |  |

**Responses**

| Status | Schema | Description |
|---|---|---|
| `200` | `any` | Successful Response |
| `422` | `HTTPValidationError` | Validation Error |


### `GET /api/autopilot/repair-previews/{plan_token}`

**Get Autopilot Repair Preview**

Return a previously created preview by token.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `plan_token` | `path` | `string` | yes |  |

**Responses**

| Status | Schema | Description |
|---|---|---|
| `200` | `any` | Successful Response |
| `422` | `HTTPValidationError` | Validation Error |


### `POST /api/autopilot/rollback/{decision_id}`

**Execute Autopilot Rollback**

Roll back the given autopilot decision if a valid snapshot exists.

Returns a structured error response with HTTP 404/409/410 when the decision
is not reversible (see `_ROLLBACK_FAILURE_STATUS` for the mapping). On
success, returns the updated snapshot, the new rollback-decision row, and
per-step outcomes so the caller can render a confirmation UI.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `decision_id` | `path` | `integer` | yes |  |

**Request body** (optional)

Content type: `application/json` — `AutopilotRollbackRequest \| null`

**Responses**

| Status | Schema | Description |
|---|---|---|
| `200` | `any` | Successful Response |
| `422` | `HTTPValidationError` | Validation Error |


### `POST /api/autopilot/snapshots/purge-expired`

**Purge Expired Autopilot Snapshots**

Admin operation: delete snapshots past their TTL.

Returns the number of rows removed. Intended for cron/scheduled use; safe
to call ad-hoc because purge logic swallows its own errors.

**Responses**

| Status | Schema | Description |
|---|---|---|
| `200` | `any` | Successful Response |


### `GET /api/autopilot/decisions/{decision_id}/snapshot`

**Get Decision Snapshot**

Return the snapshot associated with a decision, or 404 if none exists.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `decision_id` | `path` | `integer` | yes |  |

**Responses**

| Status | Schema | Description |
|---|---|---|
| `200` | `any` | Successful Response |
| `422` | `HTTPValidationError` | Validation Error |


### `POST /api/autopilot/rollback/{decision_id}/preview`

**Preview Autopilot Rollback**

Return a non-mutating preview of what rolling back `decision_id` would do.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `decision_id` | `path` | `integer` | yes |  |

**Responses**

| Status | Schema | Description |
|---|---|---|
| `200` | `any` | Successful Response |
| `422` | `HTTPValidationError` | Validation Error |


### `GET /api/autopilot/runs/{run_id}/decisions`

**List Decisions For Run**

Return every decision-log entry for one autopilot run, ordered by sequence.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `run_id` | `path` | `string` | yes |  |

**Responses**

| Status | Schema | Description |
|---|---|---|
| `200` | `any` | Successful Response |
| `422` | `HTTPValidationError` | Validation Error |
