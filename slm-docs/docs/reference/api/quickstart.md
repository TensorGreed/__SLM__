---
sidebar_position: 33
title: Quickstart
---

<!-- Auto-generated by slm-docs/scripts/generate_api_ref.py. Do not edit by hand — re-run the generator instead. -->

# Quickstart

Auto-generated reference for the **Quickstart** API. 4 endpoint(s).

For curated narrative + UI / CLI walkthroughs see the corresponding section under
[Pipeline workflows](../../workflows/pipeline-overview.md), [Deployment](../../deployment/plan.md),
[Observability](../../observability/run-events.md), or [Extensions](../../extensions/contracts.md).

### `POST /api/projects/{project_id}/quickstart/baseline-eval`

**Quickstart Baseline Eval**

Run eval against the gold/test split using the project's
un-fine-tuned base model. Creates (or reuses) a synthetic
Baseline experiment so the result row keeps its anchor in the
normal `EvalResult` plumbing; the Compare page then renders
"baseline vs. trained" side-by-side for free.

Requires:
  - project.base_model_name set (i.e. recipe applied)
  - imported / prepared data with a `test` split present

Returns the synthetic experiment_id + the eval result dict so
the UI tile can show "Baseline F1: 0.32" immediately.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `project_id` | `path` | `integer` | yes |  |

**Responses**

| Status | Schema | Description |
|---|---|---|
| `201` | `any` | Successful Response |
| `422` | `HTTPValidationError` | Validation Error |


### `POST /api/projects/{project_id}/quickstart/evaluate-latest`

**Quickstart Evaluate Latest**

Find the most recent completed (or running) training experiment
in the project and run a heldout evaluation against the gold/test
split.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `project_id` | `path` | `integer` | yes |  |

**Responses**

| Status | Schema | Description |
|---|---|---|
| `201` | `any` | Successful Response |
| `422` | `HTTPValidationError` | Validation Error |


### `POST /api/projects/{project_id}/quickstart/import-sample`

**Quickstart Import Sample**

Materialize a bundled demo dataset into the existing project.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `project_id` | `path` | `integer` | yes |  |

**Request body** (optional)

Content type: `application/json` — `ImportSampleRequest \| null`

**Responses**

| Status | Schema | Description |
|---|---|---|
| `201` | `any` | Successful Response |
| `422` | `HTTPValidationError` | Validation Error |


### `POST /api/projects/{project_id}/quickstart/train-default`

**Quickstart Train Default**

Create + start a training experiment using project defaults
(base_model_name from the selected recipe; everything else uses
the TrainingConfig Pydantic defaults).

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `project_id` | `path` | `integer` | yes |  |

**Responses**

| Status | Schema | Description |
|---|---|---|
| `201` | `any` | Successful Response |
| `422` | `HTTPValidationError` | Validation Error |
